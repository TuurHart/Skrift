mode: session (overnight run complete)
finding: Q2: mock at Skrift_Native/SkriftDesktop/mocks/three-ball-importance.html; current control is SignificanceCirclesView.swift (.phone/.mac style tables); new balls 15pt phone/iPad (was 18pt, 44pt tap target), 10pt Mac (was 13pt); amber removed with the refine wall.
finding: Q1: mock at Skrift_Native/SkriftDesktop/mocks/quick-note.html; phone has NO New Note button today (only iPad ✎ in Import·Record·✎); Lock Screen/Control Center/Siri only record today; `Memo.newTyped` saves on the tap, so an empty note would sync to the Mac — create the Memo on first keystroke.
finding: Q5: board at Skrift_Native/SkriftDesktop/mocks/Q5-long-form-inspiration.html; app pictures are wireframes from memory, not screenshots; directions A Shelf / B List / C Front Page; default tab name "Library"; Skrift parts from AudiobookLibraryView.swift, BookShelfTile.swift, Palette.swift.
finding: Q9: v1 never speech-paragraphs a voice note with a picture — `MemoSaver.runTranscription` inserts `[[img_NNN]]` BEFORE `Paragrapher.paragraphed`, which no-ops on text containing `\n`; only the marker breaks remain (violates C20; no R row yet — proposed R95 at the sitting). `BodyTransform.snappedImageBody` is the one path for display (NoteBodyView.swift:331) and export (VaultExporter.swift:132). 15 of 18 body-clause notes got expect_body.txt; 3 skipped as unresolved (see Q9 commit message). Goldens test = `BodyGoldenTests`.
finding: Q9 (toolchain): `TEST_RUNNER_*` env vars do NOT reach the host-less desktop unit-test bundle; to re-record goldens: `xcodebuild build-for-testing` once, then `SKRIFT_RECORD_GOLDENS=1 xcrun xctest -XCTest SkriftDesktopTests.BodyGoldenTests <bundle>`.
finding: Q22: mock at Skrift_Native/SkriftDesktop/mocks/one-notes-list.html; the row is ALREADY one shared view (Shared/UI/NoteCardView.swift) — differences come from what each app feeds it; double-dim (MemoCard 0.55 × NoteCardView 0.62) and Mac QueueRowView title/snippet repeat logged in BUGS.md §4; picks: pill only while working/broken, display-only balls on rows, day groups on all three.
finding: Q10: harness = BodyDiffHarnessTests + BodyInvariantTests; `BodyTransform.snapImages` is NOT idempotent on `pic-user-snapped-markers` (loses blank line after [[img_001]]) and `pic-in-task-list` (glues two task lines) — excluded as `knownSnapIdempotenceGaps`, both die with snap (C17). C19 notes `typed-crlf-tabs-nbsp`/`voice-en-triple-blank-lines` excluded from exact-match (no R row). Corpus notes SPEC cites for R25 ("migrated"), R33 ("ingress P3") and R74 (`pic-during-pause-two-shots`, `pic-burst-same-offset`) DO NOT EXIST; R74 mapped to `pic-two-same-second`; missing ones listed as `_missing_fixtures` in expected-differences.json.
finding: Q6: mock at Skrift_Native/SkriftDesktop/mocks/Q6-library-tab.html; tile tap opens/plays the source, "❝ N" pill opens its notes; capture keeps the verb "Add note" and lives only in player/mini bar/PDF reader; "Send to a note" in ⋯ + hold menu; empty tab has 4 buttons (drops the one-import-button rule in AudiobookLibraryView.swift); open question: does a sent source stay in Library.
finding: Q8: ThreeBallScale (Shared/Model) + ThreeBallImportanceView (Shared/UI) wired on Mac/phone/iPad via the SignificanceCircles.swift wrappers; old SignificanceCirclesView.swift deleted; litCount/refine pass still live in 8 non-test files + 5 protected tests (→ Q24). Q8's own check was over-broad (dispatcher's error) and was narrowed to the do: scope.
finding: Q16: recording now in `rec_seg_<take>_NNN.m4a` segments (closed every 60 s + on interruption/background/memory warning/write failure) + `rec_ckpt_<take>.json` marker; a kill loses at most the open segment (≤60 s) if iOS skips willTerminate; recovered note titled "Recovered recording (the app closed mid-take)"; os_log subsystem com.skrift.mobile category recording. Worker: 10/10 RecoverySweepTests + gate green in its tree; accept CHECK FAIL only from the _swift_initBorrow sim crash (merge undone) → needs Q25, then re-accept from worktree agent-afc3389529eaf415c.
finding: Q16 (toolchain): Xcode 27.0 / Swift 6.4 links `swift_initBorrow` as mandatory when swift-collections ≥1.7.0 is resolved; iOS 26.x runtimes lack it → dyld abort at launch (sim; device inferred). Fix = pin 1.6.0 (Q25). Report plan/research/swift-collections-initborrow.md.
finding: Q17 recipe: iPhone 13 Dev — record 90+ s, take a 20 s call, hang up, keep talking, stop → one note with speech before + after. Record 2+ min, swipe the app away mid-sentence, reopen → note "Recovered recording (the app closed mid-take)" with all speech up to the swipe (≤60 s lost if willTerminate skipped), transcribes. Console.app: subsystem com.skrift.mobile, category recording.
finding: Q4: mock at Skrift_Native/SkriftDesktop/mocks/Q4-edit-conflict.html; Keep both = default/Return; prompt opens on note open and blocks edit until picked, "Later" leaves an amber banner + "2 versions" pill; unkept version → Recently Deleted as a "replaced" row (14 d) — open question vs on-note "Other version"; assumes only body/title/tags conflict, Mac holds processing/export until picked, devices named "this iPhone"/"the Mac" (no device names stored).
finding: Q3: mock at Skrift_Native/SkriftDesktop/mocks/tag-ui-revamp.html; picks: inline field in the tag row (no sheet), tap-twice remove + 4 s Undo (Mac ✕ on hover), own tag row under the title 14 pt; three C93 violations logged in BUGS §4 (Mac lowercases on pick, no case fold, all # stripped); open question: does case fold reach across the library.
finding: Q21: `LockGate.isLocked` + new Shared `NoteVisibility`; `MemosListView.deleteMemo` is the one funnel for the 3 delete entry points; MemoDetailView Copy paths funnel through `GatedCopy.copyTranscript` (new, testable); tests LockedNoteVisibilityTests + LockedNoteCopyTests.
finding: Q23: 4 notes added (pic-during-pause-two-shots, pic-burst-same-offset, ingress-p3-five-clips-one-picture, migrated-stale-name-offsets) via ADDITIVE `manifest-q23.json` + `expected-differences-q23.json`, read only by the new `BodyGoldenQ23Tests` — the Q10 harness (BodyDiffHarnessTests) still reads only the original two files, so R74 there still maps to pic-two-same-second. `BodyInvariantTests` globs goldens/v1-body/ directly. `snapImages` is not idempotent when a deferred marker lands next to a pre-baked `\n\n`. R25 has NO check path: `CorpusSeed.Note`/`makeMemo` hardcode `nameResolutionsData = nil` (→ Q14).
finding: Q11: body v2 = 361 lines (`Shared/BodyV2/{BodyV2,BodyV2Marker,BodyV2Text,BodyV2Thumbnail}.swift`), harness 83 identical / 30 expected-different / 0 unexplained over 113 notes, idempotent on all; phone app builds (dispatcher check). Additive `expected-differences-q11.json` lists 13 notes differing for confirmed clauses with no R row (C19, C12 lone-picture-top, C14, dest-made). Carve-outs in `BodyV2HarnessTests` (`registrationConflicts`, `expectBodyConflicts`) fail once resolved: four fixtures put a picture mid-sentence against C11 (pic-at-start 0.40 s, pic-ocr-text 3.02 s, pic-three-spread #3 13.27 s, ingress-p3 10.70 s — and ingress-p3 has no clip boundaries so R33 is not yet delivered); pic-in-task-list's R95 entry is unsatisfiable under C20+D3.
finding: Q11→Q13: every write site calls `BodyV2.committed(BodyV2.Input(text:words:manifest:source:userEdited:))`, `.speech` only with real word times; drop `BodyTransform.snappedImageBody` and v1's `ImageMarkers.insert → Paragrapher` in the same swap; thumbnail = `BodyV2Thumbnail.pick`.
finding: Q17 take 2 PASSED on iPhone 13 (Dev 171, 15:34:57–15:37:42): segments n=1,2 reason=60s, n=3 reason=background at the swipe, `rec recovered sources=3`, transcribed to the last sentence ("I'm going to close the app"). Take 1 (call) owed — no SIM; use a FaceTime/WhatsApp call over Wi-Fi. DATA-LOSS: first launch deleted 5 legacy unreadable rec_tmp orphans ("cleaned 1 file(s)") and resurrected 1 old take as memo 36A5A06C → Q27 quarantine, BLOCKS any prod promotion of Q16.
resume: Q24 worker dead at shutdown — committed 576e6c09 on wt/Q24 (worktree agent-a5293a1fb407468ae); redispatch from there: run its proof, then accept by hand (only the 5 D138-approved protected tests may differ).
resume: Q27 worker dead at shutdown — 6fb419de on wt/Q27 (worktree agent-a65afc383d479ab53); still owes: success path must delete ONLY merged sources + marker and quarantine the rest; quarantine must never delete an existing copy (unique name). Then RecoveryQuarantineTests assertions for both.
resume: Q17 take 1 (call) owed — FaceTime/WhatsApp call over Wi-Fi to the iPhone 13 (no SIM). Q16 must NOT reach prod before Q27.
resume: parked for Tuur — Q3 tags mock, Q4 conflict mock, Q12 body-v2 read (https://claude.ai/artifact/C67PjceSobKrvejq3V8QkY), Q20 perf baseline. After Q24: Q26 list build; after Q12 rulings: Q13.
finding: Q27: quarantine = `Documents/QuarantinedRecordings/` beside `Documents/recordings` (never re-swept); after a rebuild only merged sources + marker are deleted, every other take file is quarantined; name collisions get a suffix, nothing inside quarantine is deleted; tests RecoveryQuarantineTests; sim flake "Application failed preflight checks" fixed by `simctl shutdown all; simctl erase "iPhone 17"`.
finding: Q18: `Shared/Naming/SafeJSONStore.swift` (rename-aside `<name>.corrupt-<ts>`, atomic write) used by NamesStore (NSLock-guarded RMW, not a Swift actor), Mac AppSettings, phone library/bookmarks/bookmark-sync blob + receiveTranscripts landed-check; "surfaced" = os_log + in-memory `CorruptFileRegistry.shared.found`, NO UI yet (banner spot: Mac Settings row / phone About). receiveEpubs/receiveAlignments not checked.
finding: Q24: litCount/SignificanceScale/refine pass gone; SignificanceScaleTests deleted, NoteConsentTests/UnratedTakeTests/SignificanceCirclesTests rewritten on ThreeBallScale (D138); SignificanceCirclesRenderTests untouched; hand-merged — worker never reported, its proof log (20:09) was green.
finding: Q13: write sites on `BodyV2.committed`: phone MemoSaver.runTranscription (+imports), diarize re-place, append, CaptureInboxDrainer annotation, NoteBodyView.commitDraft (only with a picture); Mac BatchRunner transcribe+diarize, MacMemoAuthor.markTranscribed, live-recording edited take; thumbnail `BodyV2Thumbnail.pick`. NOT switched: dictation + voice-annotate text appends, Mac text imports, Mac editor (→ Q31). Read-only fallback `Shared/BodyV2/BodyV2Legacy.swift` in 7 call sites (NoteBodyView load + applyTierStyling, BodyTextView ~169/~438/suggestedRanges, Snapshot, VaultExporter, ObsidianPublisher) — Q14 deletes it. v2 BUG: `BodyV2Text.normalised` collapses LEADING indentation too (C19 says runs INSIDE a line) → would flatten nested lists (→ Q31). Hacks to undo: typed body with editor photo passed to `pick` as `.speech`; `ASRPostProcess` still calls v1 `ImageMarkers.insert`; Mac paragraph gap moved from longFormGap to 2.0 s (C20, intended). Flaky: phone `testSavedPhotoBecomesSearchableWithoutRelaunch` (Vision OCR) failed once, passed alone.
finding: Q19: `BatchRunner.run(retranscribe:)` throws `BatchRunnerError.missingAudioFile`, clears derivatives only after the new transcript exists (merged with Q13 BodyV2 calls); `VaultAttachmentOwnership` in Shared/Export/VaultWrite.swift (byte-identical → no-op; foreign → never removed, ours lands under id8 name + embeds rewritten) on `writeAsset .file` + all 3 VaultExporter lanes. `.data` asset branch (phone MemoAsset blobs) still overwrites blind → Q32. ProcessingCoordinator is not in the MLX-free test target.
finding: Q26: shared `Shared/UI/NotesListModel.swift` (chip counts, day groups, pill rule) + `Shared/Model/MemoDate.swift` (moved from mobile); double-dim + Mac untitled-repeat fixed; phone corner mic FAB removed (Record only in verbRow); NOT visually checked (PNG step skipped); `SkriftMobileUITests` RecordingUITests + ConversationMockUITests still tap `new-recording-button` (the removed FAB) → Q33.
finding: Q32: `.data` lane via `VaultAttachmentOwnership.writeOwned`/`ownedName(matching data:)`. GAP (Q19 + Q32): when an attachment is disambiguated to an id8 name on the commit path, the note's markdown embed is NOT rewritten → Obsidian shows the foreign file's picture → Q34.
finding: Q31 (partial, resumed): dictation append, voice-annotate append, Mac text import (`IngestService.ingestNote`), Mac editor `textDidEndEditing` now commit via v2; ASRPostProcess v1 call gone. Full leading-indent keep broke protected goldens (024-typed-crlf-tabs-nbsp expect, conv-with-picture C19-only) → narrowed to list-item lines (D143). C170 pick() sub-part dropped (stopgap stays; D143).
finding: Q34: fix in `VaultWriter.commit` (Shared/Export/VaultWrite.swift): resolves each attachment owned name first, patches `![[old]]`/`](old)` embeds before the stamp; only the phone ObsidianPublisher routes attachments through commit; Mac VaultExporter lanes already write-then-patch. Phone build verified by dispatcher.
finding: Q33: phone list matches the signed mock (dispatcher looked at phone-light.png). Mac sidebar PNG shows the sidebar's LEFT EDGE CLIPPED ("ODAY", "UE 22 SEP", "All" chip, logo, search icon cut) — real or snapshot-shell artefact, unknown. The Mac PNG was rendered from the LIVE Skrift Dev store (Tuur's own Dev notes: "Recovered recording…", Dutch test rambles), not the synthetic corpus — privacy slip; branch wt/Q33 was pushed then deleted. iPad always-on "starts fading" line removed but not re-shot; Mac unrated (quiet) rows still title + spine line, not snippet + chips per the mock.
finding: Q28: `Shared/UI/TagRules.swift` + `Shared/UI/TagEditorRow.swift`; TagEditorSheet deleted; a new Shared/UI file must be listed in project.yml for SkriftDesktopTests AND the SkriftShare extension (3 places) or it silently isn't compiled; fold rule: a case-variant not on the note is ADDED under the library spelling, one already on the note is skipped. Phone build verified by dispatcher. OFF-MOCK: Undo is an inline row (mock: floating toast); Mac lost the keyboard-navigable suggestion menu (↑↓/Tab/Create row, mock has it). `SkriftMobileUITests/TagSheetUITests.swift` asserts the removed sheet + old "destination words refused" rule. No screenshot taken → Q36.
stop-when: 0 todo and 0 doing (Q15 becomes tuur once Q14 is done; tuur/stuck items never page Tuur)
plan: finish Q35 Q36 Q29 (accept each), then Q7 (quick note, signed D134/D135) and Q14 (normalise old notes once, then delete BodyV2Legacy), then set Q15 tuur "awaiting sitting".
worker: Q35 pid=- subagent (dies with the session; if no report and worktree agent-a6ba64ae64fbdcce0 idle → redispatch once from its commits) relaunches=0
worker: Q36 pid=- subagent relaunches=0
worker: Q29 pid=- subagent relaunches=0
rule: Q35/Q36 screenshots must come from the synthetic corpus in an isolated store — never the live Dev store; the dispatcher looks at every PNG before accepting.
rule: nothing is installed on a device and nothing is pushed except wt/* branches (deleted after merge); main untouched.
finding: Q35: `-snapshot-shell` now seeds an isolated in-memory store from `-corpus` (it used to open `MemoCloudStore.container` = the live Dev CloudKit store); default sidebarWidth 292; Mac quiet rows get snippet + duration chip. Dispatcher LOOKED at list-q35/mac-sidebar-dark.png: the sidebar's left ~12 px is STILL cut ("ODAY", "AT 19 SEP", logo, "All" chip, row borders) — worker's "zero clip" is wrong; cause (harness crop vs layout) unproven. Same corpus gives different chip counts: Mac "Needs Work 6 · Unrated 5" vs iPad "Needs Work 99 · Unrated 6" → Q37. iPad list-q35/ipad-dark.png matches the mock (fade line gone).
finding: Q29: each device writes its own `MemoEditHead` row per note (last title/body/tags) + `Memo.editVectorData` edit counts; conflict = neither head saw the other's edit and words differ; rating/lock/reminder/destination stay LWW; new notes never conflict; Keep both / unkept copy are TEXT-ONLY (audio/photos stay on the original). ⚠ PROD PROMOTION NEEDS A CLOUDKIT SCHEMA DEPLOY for the new `MemoEditHead` record type. MISSES: body edits on a Mac-polished note (they land in `MemoEnhancement.copyedit`, not `Memo.transcript`) → Q38; edits from older builds; possible false "2 versions" on the first post-upgrade non-word edit. Prompt/banner/pill never rendered → Q39. Unchecked: `EditConflictGate` `.disabled` on the iPad workbench.
finding (toolchain): `pgrep -f 'plan/accept[.]sh'` is GLOBAL — another project's accept.sh (stretching app, 2026-09-24 ~23:00) blocked this queue for 30 min. Briefs now wait on `pgrep -f 'Skrift/.*plan/accept[.]sh'`.
finding: Q14: `Shared/BodyV2/BodyNormaliseMigration.swift` + `Memo+BodyNormalise` (phone) + `PipelineFile+BodyNormalise` (Mac), hooked in MemoDetailView/NoteDisplayView `onAppear` (AFTER first render → `BodyV2Legacy` kept). Undo: local never-synced ledger `BodyNormalise/<id>.json` (App Support; Mac suffixed Dev/prod) = once-flag + original/migrated body; `undoBodyNormalise()` restores only if the body still equals the migrated text. Refuses unless words and markers in == out; editedAt untouched; C203 skipped by date cutoffs (image captures before 18da97ad, PDF before b41ec828/b76). R25 via Mac `ambiguousNames` re-derive; corpus `name_offsets.json`. GAPS: phone migrates only `Memo.transcript` — the polished `MemoEnhancement.copyedit` is untouched; `BodyV2.committed` keeps v1's leading space after a picture paragraph (`\n\n That`) so it still breaks C10 → Q40. Dispatcher lock note: my own accept shell matched 'Skrift/.*plan/accept[.]sh' (cd path + literal accept.sh) → self-deadlock; the dispatcher runs its accepts sequentially with no wait loop.
finding: Q36: Mac ↑↓/Tab/Return/Create menu restored (dispatcher looked: matches); floating Undo toast added BUT dispatcher looked at tags-q36/phone-undo-toast.png: the toast is anchored to the tag row — it runs off the LEFT screen edge and covers the remaining chips (mock: centred pill near the bottom) → Q41. Tag chips are StaticText in XCUITest (`app.staticTexts["tag-chip-<tag>"]`); `-selectFirstMemo` auto-opens only on iPad; Mac menu lacks per-tag counts (mock shows e.g. "31").
finding: Q40: polished copyedit migrated once via its own flag `BodyNormalise/<id>.polished.json` (set only when a polish exists); writes copyedit only — enhancedAt/enhancedByDeviceID/processedAt untouched, no markEdited/recordEdit; Mac also re-derives sanitised. Residual: a stale migration write can still race another device re-polishing the same row (CloudKit per-field merge), narrowed because both devices compute the identical rewrite. Phone polish path built, not unit-tested. v2 strips the leading run after a picture paragraph (fallback dropped).
finding: Q38: polished body counted via `recordPolishedEdit` (phone polishedBinding setter; Mac `MacCloudEditSync.flush` when body changed); Mac polish upsert + Q40 normalisation never count; Keep both / replaced copy get their own processed MemoEnhancement. New optional CloudKit fields `Memo.polishStampHash`, `MemoEditHead.polishedBody` (schema deploy owed). STILL MISSES: summary + suggested-title edits; Mac edits to unrated notes (`MemoNoteProjection.writeBack` records none); iPad per-part re-polish can overwrite LWW. RISK: `EditConflicts.recordEdit` stamps the first touch on any never-stamped (pre-Q29) note even with no word change → a first audio trim counts as an edit → possible false "2 versions" (→ Q42). Mac flush re-derives body via `unlinkToSpoken` — a title-only edit may count as a polished edit (untested).
finding: Q7: quick note = `NewNoteIntent` ("New note in Skrift"), `NewNoteWidget` (Lock Screen), `NewNoteControlWidget` (Control Center), `QuickNoteBridge`/`QuickNoteDraft`, verb-row ✎ → `newTypedNote()`; Memo created on first keystroke; empty draft dropped. `QuickNoteView` is a NEW minimal editor (title field + TextEditor, no accessory bar) — never rendered → Q43. 9/9 QuickNoteTests.
finding: Q37: chip counts now one formula (Mac Needs Work counted only PipelineFile rows; Unrated read unpipelinedMemos, not ProcessPile.unrated) — `ChipCountParityTests`. DEBUG-only `-isolatedRun` launch arg = in-memory, no-CloudKit stores for eyeball runs. Clip verdict UNSETTLED (likely `Snapshot.hostPNG` crop; real window needs an unlocked screen).
finding (ENV, HARD): `open -a "<worktree>/Skrift Dev.app"` launches the REAL /Applications/Skrift Dev.app (same bundle id com.skrift.desktop.dev) — it touched the live Dev CloudKit store once (2026-09-24 night; only an already-done idempotent migration could run). Run the raw `Contents/MacOS/Skrift Dev` binary with `-isolatedRun`. A worker's whole-screen `CGWindowListCreateImage` probe captured a private WhatsApp photo from Tuur's desktop (deleted; not in any commit). Every brief now forbids `open -a` for Skrift apps and any whole-screen / all-windows capture. Real-window captures need an unlocked screen; the sim renders offscreen.
finding (ENV): parallel workers on the shared "iPhone 17" sim cause false failures ("Application failed preflight checks", SwiftCompile exit 0 no output) at load ~60 → run sim-heavy items one at a time.
finding: Q41: toast hoisted — `TagEditorRow` reports `TagToast` via `onToast`; screens own `@State tagToast` + shared `TagUndoToastView` (like `NameUndoToast`); Mac menu shows `TagLibrary.counts`. Dispatcher LOOKED at tags-q41/phone-undo-toast.png: centred horizontally but floats MID-SCREEN over the Importance card ("Rated — rea|dy" covered), not just above the player/keyboard → Q44.
finding: Q42 (partial, merged): Mac `MacCloudEditSync.flush` compares the polished body through the process→unlinkToSpoken round trip (title-only edit ≠ polished edit). The first-touch fix inside recordEdit/recordPolishedEdit is IMPOSSIBLE without breaking 9 protected assertions (EditConflictTests/PolishedEditConflictTests seed never-stamped notes and rely on the first stamp being a real edit) → do it at the call sites instead: audio trim / annotation / other non-word `markEdited` callers pass `stampWords: false` like `ReminderSheet` (→ Q45).
finding: Q45: stampWords:false on lock (detail+list), destination pick, name link/unlink/undo, voice annotation, and polished commits (recordPolishedEdit stamps those); `NoteBodyView.onCommit` now carries `wordsChanged`. Audio trim and photo add/remove/markup never call markEdited. `MemoEditHead.detect()` treats nil baseHash as changed by design.
finding: Q39: conflict UI checked by the dispatcher against the signed mock (phone prompt, banner after Later, "2 versions" pill, Mac prompt after fix: wrapping subtitle + shared pillButton default). DEBUG seams: phone `-forceEditConflict`, Mac `-snapshot-conflict`. hostPNG misdraws `.bordered` system buttons and truncates unwrapped text. iPad pager realises the adjacent page, so `detail-title` matches twice in XCUITest — index into the set.
finding: REGRESSION from Q45 (merged 355a60aa): `NoteBodyView.onCommit` became `(Bool) -> Void` (wordsChanged); protected `SkriftMobileTests/NoteBodyTests.swift` + `QuotePresentationTests.swift` pass `onCommit: {}` (17 sites) → "contextual type for closure argument list expects 1 argument" → the WHOLE phone test target fails to build on the session branch, so every `plan/mtest.sh` fails. Q45's proof built the app only, not the test target; the gate is desktop-only. Rule: any item touching phone code proves `xcodebuild build-for-testing` for SkriftMobile, not just `build`. → Q46.
finding: Q43 (blocked by the regression): QuickNoteView reshaped like the editor (compact nav bar, `QuickNoteBodyTextView` with the same `NoteAccessoryBar`), `QuickNoteDraft.discard(context:)`; screenshot never taken. Worktree agent-aa06e0ed307fea7ce, branch wt/Q43, 2 commits.
finding: Q46: `NoteBodyView.Coordinator.init(memo:onCommit: () -> Void)` convenience init forwards as `{ _ in onCommit() }` — phone test target builds again. ENV: two simulators are both named "iPhone 17" (4962056D…, B7B7068C…); the "Busy — Application failed preflight checks" launch flake needs `xcrun simctl boot <UDID>` + ~5 s wait before xcodebuild test (shutdown+erase alone is not enough). Dispatcher pre-boots both before any phone-check accept.
finding: Q43: QuickNoteView reshaped like the editor (QuickNoteBodyTextView + NoteAccessoryBar); dispatcher looked at quicknote-q43/phone-quick-note-empty.png: matches the mock (plus/ellipsis appear only once a keystroke creates the Memo — deliberate). A container `.accessibilityIdentifier` with no AX node leaks onto children; the SwiftUI identifier beats a UIKit one set in makeUIView; a fresh sim shows the one-time "slide to type" tip over the keyboard.
finding: Q44: toast anchored with keyboard tracking in MemoDetailView (6 pt above player/accessory with keyboard up, 96 pt with keyboard down); dispatcher looked at both tags-q44 PNGs: no content covered on the 2-line fixture; on a long note the toast would sit over text (no scroll reservation) — minor, not queued. `.padding(.bottom)` on a bottom overlay moves it UP-screen.
finding: Q60: Periphery 2.21.2 (release zip in ~/bin; the brew cask refused on a macOS-version check). 374 findings: SAFE 245 (~1,976 lines, upper bound), CHECK 127 (116 built+tested but never called from app code; 11 structural: AppIntents/@Model/Codable/DEBUG), KEEP 2 (pruneOldTombstones, AudiobookAsset init). Test-only reclassification is filename+grep heuristic — re-verify before deleting.
finding: Q50: mock at Skrift_Native/SkriftDesktop/mocks/Q50-compact-note-header.html (artifact https://claude.ai/artifact/D281U7Pwj9RpMxapaGRZDi); place chip is set only by `MetadataService.capture()` (RecordView:160, MemoSaver.applyMetadata:740) — typed notes, imports and captures never get a place; a voice note's nil GPS/geocode is dropped silently by `LocationOneShot`. Options A/B/C: phone voice header 243 pt today → 133/127/127 pt; drift: phone date has time no glyph, Mac date has glyph no time.
finding: Q50 (toolchain): two `xcodebuild test -scheme UnitTests` runs from different worktrees at once killed the accept gate at "Resolve Package Graph" (shared SPM cache) — RED with no test run. Accepts wait for `pgrep -f 'xcodebuil[d].*Skrift'` to clear; worker proof lines also wait on `xcodebuil[d].*SkriftDesktop`.
finding: Q56: backlinkedIDs cached @State once per list refresh; ProcessingCoordinator.export async (VaultExporter I/O in Task.detached) at 4 call sites — the 4th was NoteActions.swift:132, not in the do:; TagLibrary counts cached; BodyTextView restyles only the edited paragraph, full pass + modelString + model write debounced 1 s, decisive edits flush via flushModelCommit. Trade-off: typing inside a heading/link shows plain styling up to 1 s (never rendered on screen) → Q63.
finding: Q56 (toolchain): the gate's `SkriftDesktopTests` does NOT compile `SkriftDesktop/Features/` (project.yml cherry-picks pure-logic files) — a green gate says nothing about Mac UI code; any item touching Features/ must also run `xcodebuild build -scheme SkriftDesktop -destination 'platform=macOS' -skipMacroValidation`.
finding: Q47: `QuickNoteView` NOT retired — MemoPageView needs a persisted @Bindable Memo, and eager-creating one breaks D91; instead QuickNoteView reuses the editor's chrome (ContextChip date, TagEditorRow, SignificanceCircles, NoteAccessoryBar) on local @State until the first keystroke. Old-note bug: MemosListView compared the pushed UUID against a separate `quickNoteDraftID` — two states that could desync after crash-recovery relaunch; now one `NoteRoute` enum (.draft/.memo, Services/QuickNote/NoteRoute.swift). Toolbar fix = NoteAccessoryBar intrinsicContentSize, a guess. Both device-unverified → Q64.
finding: Q47 (queue): an item whose do: requires a NEW test file under SkriftMobileTests/SkriftDesktopTests needs `gate+: yes` or accept.sh REJECTs it (Q16 hit this too).
finding: Q49: mock at Skrift_Native/SkriftDesktop/mocks/Q49-one-filter.html (artifact https://claude.ai/artifact/Y3MiDANfviMmWcC66mRDtc); worker's pick A = chips carry everything (Date + Unsynced chips, sort as a tap-cycling word, no icon; row needs a swipe at 390 pt); B hides counts behind one button (reverses D135); C drops date range + Unsynced (contradicts the 2026-07-23 keep). Bug: phone filters Unrated twice (chip + sheet toggle) → BUGS §2. Mac Filter icon may clip at the 292 pt sidebar (unverified).
finding: Q57 (toolchain): plan/mtest.sh always uses the ONE "iPhone 17" sim — two phone test runs from different worktrees at once may fail each other (Q57's own red was NOT this); worker proof lines and accepts wait on `pgrep -f 'xcodebuil[d].*SkriftMobile'`. Accepts are serialised with `/usr/bin/lockf -t 7200 /tmp/skrift-accept.lock`.
finding: Q57: localAlignmentSignature served from a BookAlignmentStore signature cache; mergeSentences linear (incoming batch is always one textFile per call); AudiobookSession @Observable (9 view files lose @ObservedObject); SharePayloadLoader loads via TaskGroup, order kept, and is now a multi-target source in project.yml (like CaptureInbox.swift). Its accept red was a test bug: UIGraphicsImageRenderer without a format draws at the sim's 3x scale — tests that measure rendered pixels set format.scale = 1.
